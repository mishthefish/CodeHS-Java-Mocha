public String toUpper(String str)
{ 
    String result = "";
    for(int i = 0; i < str.length(); i++) 
    {
        char cap = Character.toUpperCase(str.charAt(i));
        result += Character.toString(cap);
    }
    return result;
}
